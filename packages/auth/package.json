{
  "name": "@repo/auth",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/auth.ts",
  "types": "./src/auth.ts",
  "scripts": {
    "generate": "pnpm dlx @better-auth/cli@latest generate --config ./auth.ts  --output ../../packages/db/src/auth-schema.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "better-auth": "^1.4.18",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.1"
  }
}
